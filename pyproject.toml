[project]
name = "vulcano-app"
version = "0.1.0"
description = ""
authors = [
    {name = "m22600",email = "pacheco.raimundo@marinha.pt"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask (==2.3.2)"
]

[tool.poetry]
packages = [{include = "vulcano_app", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
