apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vulcano-app-pvc
  namespace: dsi-paas
  labels:
    app: vulcano-app
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: thin-csi
  resources:
    requests:
      storage: 2Gi
