apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vulcano-db-pvc
  namespace: dsi-paas
  labels:
    app: vulcano-db
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: thin-csi
  resources:
    requests:
      storage: 10Gi
