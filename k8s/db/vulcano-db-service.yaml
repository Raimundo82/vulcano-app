apiVersion: v1
kind: Service
metadata:
  name: vulcano-db-service
  namespace: dsi-paas
  labels:
    app: vulcano-db
spec:
  ports:
    - port: 3306
  selector:
    app: vulcano-db
    tier: mysql-db
  clusterIP: None
